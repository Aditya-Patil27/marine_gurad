# ml

Training code, dataset preparation and model cards for the models the API loads (YOLOv8 SAR hull detector, LSTM route model, hotspot ranker). Trained weights are not committed; the API reads them from `apps/api/models/` or the model registry.
